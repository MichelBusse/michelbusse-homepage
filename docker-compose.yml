version: "3"
services:
  michelbusse-homepage:
    container_name: michelbusse-homepage
    image: m-to-b/michelbusse-homepage
    restart: unless-stopped
    networks:
      - swag-net

networks:
  swag-net:
    external: true
